# ansible/inventory.yml
all:
  hosts:
    production_server:
      ansible_host: "{{ lookup('env', 'DEPLOY_HOST') }}"
      ansible_user: "{{ lookup('env', 'DEPLOY_USER') }}"
      ansible_ssh_private_key_file: /tmp/ssh_private_key